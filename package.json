{
  "name": "connectnow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build"
  },
  "engines": {
    "node": ">=20.0.0 <22.0.0"
  }
}
